# Number-Guessing-Game
Java-based application where the system generates a random number and user tries to guess it within a limited number of attempts.
features:
Random number generation(1-100)
user input handling
hints like "too high" or "too low"
limited attempts for better gameplay
